{{#unless view}}<a href="/Monsters/view/{{monsterId}}">{{/unless}}
    <div class="card monster {{# if view}}full static{{/if}}">
        <div class="title">
            {{monsterName}} ({{healthPool}} hp)
        </div>
        <div class="description">
            {{#unless view}}<div class="stats">{{attack}} ATK | {{defense}} DEF | {{speed}} SPD</div>{{/unless}}
            {{monsterDesc}}
        </div>
    </div>
{{#unless view}}</a>{{/unless}}