{{#unless view}}<a href="/Quests/view/{{questId}}">{{/unless}}
<div class="card quest {{# if view}}full static{{/if}}">
    <div class="title">
        {{questName}} {{#unless view}}{{#if available}}(!){{/if}}{{/unless}}
    </div>
    <div class="description">
        {{questDesc}}
    </div>
    <div class="giver">
        {{#if questGiverId}}- {{questGiverId}}{{else}}- Unknown{{/if}}
    </div>
</div>
{{#unless view}}</a>{{/unless}}