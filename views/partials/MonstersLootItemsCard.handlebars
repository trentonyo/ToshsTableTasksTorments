{{#unless view}}<a href="/MonstersLootItems/view/{{monsterId}}-{{lootId}}">{{/unless}}
    <div class="card loot jsp-loot-light-backgroundColor jsp-loot-light-thickerBorderColor {{#if view}}full static{{/if}}">
        <div class="title animate-background-gradient jsp-global-black-textColor" data-jsp-palette="monster">
            {{monsterName}}
        </div>
        <div class="description jsp-loot-dark-textColor">
            {{#unless view}}<div class="stats">{{dropQuantity}} Dropped | {{#localizeChance dropChance}}{{/localizeChance}}</div>{{/unless}}
            {{lootName}}

        </div>
    </div>
{{#unless view}}</a>{{/unless}}