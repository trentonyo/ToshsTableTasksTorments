<script src="/cards.js" defer></script>

<div id="page_title" class="fadeout-gradient">{{queryName}}</div>

<div id="card_container">
    {{#each results}}
        {{> (whichPartialCard) }}
    {{/each}}
</div>
